spring:
  application:
    name: store

  profiles:
    active: test

management:
  endpoints:
    web:
      exposure:
        include: "*"  # 或 "health", "info" 等
  endpoint:
    health:
      show-details: always